# OMP Desktop 执行约定

本项目由规划模型准备，由用户选择其他模型执行。默认按单个阶段推进，不自动启动其他代理或新的任务。

## 开始之前

1. 阅读 `HANDOFF.md`、`docs/00-scope-and-decisions.md` 和本阶段文档。
2. 执行根目录 `git status --short` 和 `git submodule status`；需要参考源码时先初始化子模块，保留用户已有改动。
3. 检查各源码仓库根目录是否存在 `.codegraph/`；若存在，优先用 CodeGraph 理解和定位代码。若不存在，不自行建立索引。
4. 进入 `app/` 或 OMP 仓库修改代码前，继续阅读对应仓库及子目录适用的 `AGENTS.md`。本文件不替代上游约定。
5. 使用现有 NVM：`nvm use 24`。非交互 shell 可先加载 `$HOME/.nvm/nvm.sh`。不要改用户全局默认版本。

## 项目边界

- 根仓库统一管理 `app/` 产品源码和 `docs/` 计划；`upstream/pi-desktop/` 与 `upstream/oh-my-pi/` 是固定提交的参考子模块。新的独立请求在本项目根仓库建立专用分支及 worktree，随后在其 `app/` 中开发，不在上游子模块中改产品代码。
- 初期保留 PI-Desktop 的布局和交互，不进行无关的 UI 重写。
- 不用字符串替换的方式将全部 Pi 包替换为 OMP 包。
- 不通过抓取终端 ANSI 文本实现桌面聊天。使用结构化 SDK/RPC 事件。
- 不在多个组件中散落 OMP 判断；把运行时差异集中在边界和适配层。
- 根仓库 `origin` 是用户的 `MisterBowie/omp-desktop`。上游子模块只用于获取参考源码，不把 `vastsa/PI-Desktop` 或 `can1357/oh-my-pi` 当作用户推送目标。
- 不因示例配置而调用真实付费模型。普通自动化测试使用 mock；真实烟测使用用户配置的测试模型和明确预算。

## 必须守住的行为

- OMP 执行不能悄悄绕过现有权限语义。普通 RPC 中是否存在完整的工具前置阻断和回传能力，必须实测。
- RPC UI 提问不自动等同于安全审批。不能只增加一个弹窗而让工具已经执行。
- 取消必须作用于后台任务和子进程；晚到事件不能让已终止任务重新变为运行中。
- 会话必须绑定创建时的项目、引擎和运行时版本。切换侧边栏项目不改变后台会话的工作目录。
- OMP 原生工具的结果、子代理和事件要保留其结构信息；未知结果可退化为通用展示，但不能伪造成已支持的专用交互。
- OMP 与 Pi 的 MCP、记忆、规则、模型配置和任务编排需要单一责任方；不重复加载或同时调度。
- 新应用使用独立的数据目录、应用标识、凭证存储及更新源；隔离包括已有文件型 secrets，使用系统密钥存储时也应隔离其命名。开发和测试不得自动迁移或覆盖用户的 PI-Desktop/OMP 数据。
- 密钥不进入 Git、截图、日志、协议样本和测试快照。

## 每个阶段的完成标准

1. 先定位真实实现，再更新设计；记录与原计划不同的事实及原因。
2. 只实现当前阶段及必要依赖，保持变更可审阅。
3. 运行与风险匹配的测试，记录命令、退出状态、运行时版本和重要输出。
4. 将状态更新到 `docs/04-task-board.md`，将证据写入 `docs/validation/`。不得把未运行的测试写成通过。
5. 阶段交接说明：完成了什么、改了哪些文件、如何验证、未解决的问题、下一任务。
6. 没有接口或权限证据时，完成独立任务并清楚标记阻塞，不用“后续完善”掩盖关键缺口。

## 文档与源码一致性

`docs/source-baseline.json` 记录首次拉取基线；升级时新增日期化的升级记录，不覆盖历史事实。文档中的源码行号用于该基线定位，更新后必须重新核对。

本工作区的规划和交接文档用中文；修改 `app/` 中的代码、规格和 ADR 时遵循上游英文要求。OMP 源码构建要求以根配置为准：Bun >=1.4，Rust nightly-2026-08-12；桌面 Rust 工具链单独管理。

许可证和上游版权声明必须保留。应用改名、图标、分发策略单独处理，不通过删除声明完成品牌替换。
