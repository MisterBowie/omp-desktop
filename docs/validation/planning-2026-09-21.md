# 规划交付核验记录

日期：2026-09-21。范围仅限项目准备、源码基线与文档，不是应用接入验收。

本文件保留首次规划的历史记录。随后用户要求推送 GitHub，根仓库已改为直接管理 `app/`，原 worktree 保留于本机 `.local-worktrees/pi-desktop-baseline/`，上游参考改为固定提交的子模块。当前使用方式见根 README 与 HANDOFF。

## 已执行

| 检查 | 结果 |
| --- | --- |
| 官方 PI-Desktop main 浅克隆 | 成功；SHA `0111e306c120ad5820688d7608cb37bad8fbcc1f` |
| 官方 OMP main 浅克隆 | 成功；SHA `d49918fab2dba3986927f2d46721629ed0f3a02c` |
| PI-Desktop 专用 worktree | `app/`，分支 `codex/omp-desktop`，与主克隆共享同一基线 |
| 本地跟踪文件数量 | PI-Desktop 2093；OMP 7518 |
| `.codegraph/` 检查 | 两个仓库均不存在，未自行索引 |
| 三个源码目录 `git status --short` | 均为空；未修改应用或 OMP 源码 |
| `git -C app diff --check` | 退出码 0 |
| 两个上游主克隆 `git fsck --connectivity-only` | 均退出码 0 |
| `git -C upstream/pi-desktop worktree list --porcelain` | 正确列出主克隆 main 与 app 开发分支 |
| 根规划文档相对链接和源码行号范围 | 最终 12 份文档、63 个本地链接全部有效；24 个实现任务完整 |
| Markdown fenced code blocks 与行尾空白 | 最终检查通过 |
| `docs/source-baseline.json` | JSON 解析成功；SHA、文件数与本地 Git 核对一致 |
| Node 24 实际文件执行 | `/Users/vv/.nvm/versions/node/v24.14.0/bin/node --version` 输出 v24.14.0 |

网络直连下载遇到低速和一次 HTTP/2 中断，随后使用系统已经配置的 `127.0.0.1:7897` 代理重新克隆成功。没有改全局 Git 或网络配置；未把未完成下载当作源码交付。

## 环境观察

- 默认 Node：v20.10.0；用户确认通过 NVM 切换 24。
- 本机可用 Node 24：v24.14.0。
- 当前 pnpm：10.33.0；PI-Desktop 要求 10.34.5。
- 当前 PATH 和标准用户目录没有发现 Bun/Rust，未在本轮安装。
- OMP 根 packageManager：Bun >=1.4；coding-agent engines：Bun >=1.3.14。
- OMP 源码 Rust：nightly-2026-08-12。

## 未执行

- 依赖安装、TypeScript 构建、Rust 构建、产品单测/E2E。
- OMP RPC/SDK 实际联调及工具权限、取消、恢复实验。
- 真实模型调用、产品打包、签名、公证和发布。

原因：用户要求本轮规划和拉取代码，使用其他模型执行实现。下一阶段为 M0，产品任务均保持“待开始”。

本工作区文档仓库尚无提交，文件保存在磁盘且可直接使用；用户未要求提交。两个上游仓库保持其原始许可证、版权和代码。
